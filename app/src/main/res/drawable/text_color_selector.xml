<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:color="@color/UnitaYellow" android:state_pressed="true"/>
    <!-- Default state -->
    <item android:color="@color/white"/>
</selector>
